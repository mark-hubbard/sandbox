step 1, from project repository bring the changes and test
git fetch origin
git checkout -b y10 origin/y10
git merge master

step2 Merge the changes and update on GitHub
git checkout master
git merge --no--ff y10
git push origin master

git checkout -b aa origin/aa does not work
error says can't checkout and switch to a new branch??

git checkout -b y0 origin/y0 
Dec 9:13am
